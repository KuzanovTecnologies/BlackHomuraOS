#include <filesystem.c>

if system_filesystem "invoke" [system_filesystem];

import system filesystem[system_interface];

/Add System Filesystem/

if S1 == S2 "import system filesystem" [invoke_system _filesystem_user_interface(UI)];

S1 == import_system_filesystem;
S2 == import_system_user_interface_filesystem;

int def == customize_system_filesystem_UI;

if customizing system UI == then include:
(1. Wayland)
(2. Hyperland)
(3. Waybar)
(4. LXQT)
(5. Hyperium)