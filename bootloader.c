#include <bootloader.c>

/import bootloader/

if S1 == S2 "add system bootloader"

S1 == "add system bootloader"
S2 == "invoke system bootloader"

if customizing bootloader == then include:
(1. additional system bootloader)
(2. enhanced system bootloader)
(3. dedicated system bootloader)
(4. channelized system bootloader)
(5. integrated system bootloader)