#include <sys.c> 

import sys: "add_sys_additional_dependencies"

/add sys additional dependencies/

if S1 == S2 "add sys additional dependencies"

S1 == "add sys additional dependencies"
S2 == "invoke sys additional dependencies"

if customizing sys == then include:
(1. sys recovery units) // For DDoS Attack Methods
(2. fallback systems) // For system recovery
(3. system signatures failsafe) // For internal components recovery 
(4. system regenerators) // For troubleshooting
(5. system refactors) // For manual system recovering