#include <os.c>

import os: "add_operating_system"

/execute linux distribution compilation/

if S1 == S2 "execute compilation"

S1 == start compiling linux distribution at 100% filesystem build
S2 == compile all the system dependencies and generate the system image disk (.iso) with every possible files and dependencies

if customizing os == then include:
(1. osprober_auto_generators)
(2. native AMD64/Intel-X86_64 Graphics Driver Support)
(3. Additional Open_Source/Proprietary Drivers for graphics)
(4. Full Data Size for graphical memory)