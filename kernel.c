#include <kernel.c>

/import kernel/

if S1 == S2 "add system kernel"

S1 == "add system kernel"
S2 == "invoke system kernel"

if customizing kernel == then include:
(1. additional system kernel)
(2. enhanced system kernel)
(3. dedicated system kernel)
(4. channelized system kernel)
(5. integrated system kernel)