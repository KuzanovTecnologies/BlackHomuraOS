#include <string.c>

import string: "add_string_algorithm_variables"

/include string algorithm variables/

if S1 == S2 "include algorithm variables"

S1 == start by including string dependencies with all possible algorithm variables
S2 == generate all the possible algorithm variables before the system starts installing and with all the dependencies included

if customizing string == then include: 
(1. system variables)
(2. system secure probers)
(3. system firewall relays)
(4. security design for the system)
(5. additional fallback systems for further defense layers)