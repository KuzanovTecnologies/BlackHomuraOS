#include <stdlib.c>

import stdlib: "add_stdlib_resources"

/add stdlib resources/

if S1 == S2 "add stdlib resources"

S1 == "add stdlib resources"
S2 == "invoke stdlib resources"

if customizing stdlib  == then include:
(1. additional standard libraries)
(2. common library values)
(3. enhanced resource libraries)
(4. dedicated resource libraries)
(5. resource library prober)